enum UserState { intial, loading, success, error, networkError }

enum RegistrationState { intial, loading, success, error, networkError }

enum GetAllNotes { intial, loading, success, error, networkError }

enum GetNoteStatistics { intial, loading, success, error, networkError }

enum CreateNewNote { intial, loading, success, error, networkError }

enum UpdateNote { intial, loading, success, error, networkError }
